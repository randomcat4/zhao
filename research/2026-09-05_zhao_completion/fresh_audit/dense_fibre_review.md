# 稠密纤维证明独立审计

**STATUS: CRITICAL_GAPS**

## GAPS

1. **PR #1 中缺少承重的根表，按所提交文件不能执行端到端重放。** `run_all.py` 在生成 `rank3_roots.regenerated.txt` 后，立即将它与证明目录中的 `rank3_roots.txt` 作逐字节比较，随后两份完整四维搜索及 `verify_roots.py`、`negative_controls.py`、`verify_logs.py` 都继续读取该固定根表；但 `rank3_roots.txt` 不在 Git 跟踪文件中。实际运行当前提交的 `verify_roots.py` 和 `negative_controls.py` 均因该文件不存在而以 `FileNotFoundError` 退出。`UPLOAD_SCOPE.md` 也明确承认该根表没有上传。因此 PR #1 自身所称的 1,786 个根及其四维全扩展不能由声明的单条重放入口认证。

2. **所附 `execution_report.json` 不能绑定当前提交中的完整校验链。** 对 Git 提交对象本身计算 SHA-256（排除了 Windows 换行转换影响）后，报告记录值与提交值不一致的承重文件包括 `audit_interfaces.py`、`farkas_certificate.json`、`verify_farkas.py`、`verify_logs.py`、`verify_roots.py`；报告列出的 `rank3_roots.txt` 则完全缺失。两份逐根原始搜索日志也未提交，现有 `verify_logs.log` 只是 686 字节的汇总 JSON，不能让 `verify_logs.py` 重验 1,786 行逐根分母、候选池、退出码与节点账。因此旧的 `NORMAL` 摘要和总分母相等式不是当前文件版本的可核验证据。

3. **后加的补救工作流尚未形成成功证据。** 当前审计分支 `3a60099933320e638e20ec27407b57a726c51b6b` 增加了 GitHub Actions 工作流，先由 `generate_roots.cpp` 重建遗漏根表，再调用 `run_all.py`；根表重建步骤已经成功。但审计时运行 `33987370459` 的“完整独立重放”仍为 `in_progress`，没有成功结论或可下载的完整日志。它也不是 PR #1 的 `b500136` 提交内容。在这次完整重放成功且日志工件可读取以前，不能把承重的 406,416 个三维对象、1,786 个根、两套四维搜索和总分母 `17972093794219884182340301482` 视为新鲜认证通过。

## 已核准但不足以消除上述缺口的部分

- **量词覆盖没有发现逻辑断层。** 虽然第 9 节的逐值表只列 `m=20,25,...`，第 4--8 节的几何归约和四维搜索没有使用 `m>=20`，而是直接排除任意 21 元平方自由短零和反例；所以若承重搜索确实完整成功，它也覆盖可能的 `m=15`，并给出完整平方自由 A。第 9 节的 23,253 个数只是在冻结的 `m>=20` 列表内完成计数，不是覆盖 `m=15` 的理由。
- **`B_sf` 手证自包含。** 十九点层先以固定和值二位置匹配的至多四边界排除十七项零和；十八点层同理排除十六项零和；每个十七点删除层遂恰有一个十五项零和，五位置补集给出 `3-(20,5,1)` 设计，而 `6d(x)=C(19,2)=171` 矛盾。这里没有调用旧 B 计算或把序列重数结论偷渡到集合情形。
- **Farkas 方向与整数边界通过实际检查。** 用当前提交的 `verify_farkas.py` 实跑，全部 9,590 个未取商整数轮廓均满足 `P(n)>=0`，最小值为 0，且在 `n_0=6`、其余 15 点集中于一个非零纤维的四个轮廓达到；求和右端为 `-2844400`。系数 `a_14,...,a_17` 全为 0，所以未知的十四至十七项零和数没有被错误置零。`audit_interfaces.py` 也实跑通过全部 3,125 个删除剩余类、7,547 个删除索引和 9,364 条方程的计数。
- **源码层面的有限分类与分枝账本未见立即可构造的漏分支。** 三维生成器从每个有序基可归一化到标准基这一事实出发，轨道清除的分母设计完整；四维第二校验器以最大下标分枝、正确着色的团上界和逐节点二项式账本覆盖原始子集，使用无符号 128 位整数，方向正确。这个静态结论不能替代缺失的根输入和未完成的全运行。

## 裁决

当前没有找到手写数学链中的确定反例；关键问题是 PR #1 尚未提供与当前源码版本相绑定、可成功重放的完整有限计算证据。只有补救工作流以成功状态结束，并保留根表哈希、两份 1,786 根全日志、全部返回码及 `verify_logs.py` 的最终成功输出后，才可重新考虑 `CORRECT`。
