# 七双重值局部命题与 H12 分类：新上下文独立审计

STATUS: CORRECT

JUSTIFICATION: 收到的冻结版本完整证明了 `g1²…g7²r1…r6` 二十位置序列存在长度至多 13 的非空零和，并完整推出六个互异值各两份的零和自由 H12 只有一个 GL3 轨道。正规化覆盖、完整有限分母、全域候选、着色证书、十五残余的实际位置覆盖、稠密十二项接口及分类反向嵌入均无关键缺口。以下独立整数核验实际运行成功；未把作者另一份回放当作独立审稿。

审计日期：2026-09-04。工作目录为 `C:/game/gameproject/showa100/math/2026-09-04_zhao_attack`。未访问 `C:/canglan`；未创建子智能体；未修改收到的命题、生产脚本或证据。全部新工件使用 `verify_seven_fresh` 前缀。

## 1. 冻结对象和结论边界

被审核证明为 [seven_doubles_continue.md](C:/game/gameproject/showa100/math/2026-09-04_zhao_attack/proofs/seven_doubles_continue.md)，SHA-256：

    dcdabbdac9bba313b2ee093d7d5efdc929ecc3159041f75860db0242c51ab7c6

局部命题是该稿第 11–19 行；附加分类是第 130–138 行。冻结清单为 `evidence/seven_doubles_continue_frozen_manifest.json`。A21、B20 的目标仍分别为长度至多 13、14 的非空零和，序列允许重复并按位置计数。

在公共已证高度界 h≤3 下，a=0、b=7 的 B20 序列正好是七双重值加六个互异新单项；A21 则有七个新单项，可删去任意一个。因此本局部命题排除两个端点中 a=0、b=7 的整个实际重数类。结合既有 2a+b≤7，只能更新 a=0 分支为 b≤6，不能由此宣称 A/B 全题已证明。分类结论也仅覆盖六种双重值的十二项序列。

## 2. 纸面论证逐点核验

**第 23–35 行：秩与首根规范化。** 增广理想幂零论证确给 D(F5^r)≤4r+1，适用于重复位置；rank(T)≤3 时十三位置中已有所需零和。rank(T)=4 时任取四个支撑基做线性变换，保留七种值各两份。S4 保持固定八项基核心。选择轨道最小值最小的新增块 u，并将它送至该最小编码后，其他像至少为同一编码；由于原支撑互异且变换可逆，实际不能等号。因此后两块严格大于首根，递增排序覆盖每个安全核心。无需把后两块也限制成轨道代表，也不需要完整 GL4 去重。

**第 39–49 行：块判据和完整节点。** 新零和若使用一份 x，旧核心须表示 −x 且长度≤12；若使用两份，须表示 −2x 且长度≤11。因此安全条件恰为 d(−x)≥13、d(−2x)≥12。0 的空表示正确排除 x=0。距离更新从旧表读取，不能重复使用新增位置。安全最终核心的每个前缀必安全；初始安全候选剪枝因而有效。十四项零和允许存在，不能误删。本次独立生成在每个十四项节点立即检查，不以是否终端叶为条件。

**第 53–65 行：图证书。** 新支撑单项候选确为 x∉supp(T) 且 d(−x)≥13，不能只用沿树遗留的候选。两个单项各自安全时，Txy 新增短零和必须同时使用它们，所以边条件恰为 d(−x−y)≥12。特别是 x+y=0 时空核心表示使该边消失。真实安全六单项扩张必形成六点团；一个给定的合法至多五着色足以排除。无需反向断言团就可扩张，也未把残余 124 色解释成色数的下界。

**第 95–114 行：实际 H12 与矩阵。** 本次重新覆盖全部 624 个非零线性泛函，归一为 156 个互异核，每个核含 125 个群元素。十五个残余各有且仅有一个含十二位置的超平面；其内部恰为六种值各两份。字面位置枚举确认无非空零和，所有 125 点可达，σ≠0 且距离 12，其余距离≤6。全部给定最短表示和 4×4 矩阵均核验，矩阵在 F5 上可逆、外部值像为 e1、内部支撑像为指定参考集合。没有借用先前九核心清单的穷尽性。

**第 118–126 行：旧稠密接口。** 本轮十五个 V 满足已经认证的抽象引理全部前提。反设 S 无≤13零和，则由 D(H)≤13 得 H 内恰好十二位置，另八位置商群值非零。每个长≤7的非空商群零和 Q 的实际和必须为 t=−σ(V)，否则由 V 中≤6位置消去；两部分位置不交，允许取 V 的空子序列而 Q 保证非空。两个不交商群零和且总长≤7时，其并集也受同一约束，导致 t=2t，与 t≠0 矛盾。六个同商值时，比较五位置子集得实际值全相同，任选五份在指数五群中和为零。重数、非空性和总长度条件均正确。本次还独立检查了八个非零 C5 位置的全部 165 个重数向量，确认该组合分岔。

**第 130–138 行：GL3 分类覆盖。** 任意所述 U 必秩三，否则 D(F5²)≤9 矛盾。取 e∉H，T=e²U 零和自由：任何含外部位置的子序列商群系数只能为 1、2。T 满秩四，故进入已完全覆盖的表。对于新项 x∈e+H 或 2e+H，含 x 的子序列商群系数分别为 {1,2,3}、{2,3,4}，故这 250 个值全部单独安全。两陪集互异，与原支撑的交恰为 {e}，扣除它得到至少 249 个核心外候选。候选数在线性变换下不变。本次独立确认候选数大于 45 的键集合恰好等于十五残余，且每个为 251。规范化后 U 的像是十二项超平面部分，唯一性将其识别为证书中的 V；给定矩阵与规范化的复合限制到原 H，确为所需 GL3 同构。该推论没有遗漏另一个可能的 H12 超平面。

## 3. 全量独立计算

新审计器 [verify_seven_fresh_core.cs](C:/game/gameproject/showa100/math/2026-09-04_zhao_attack/scripts/verify_seven_fresh_core.cs) 不导入作者生产程序或任何旧验证引擎。它直接枚举基系数 {0,1,2}^4 和每个新增块的系数 {0,1,2}，从所有 3^7 个实际重数选择重建每个十四项核心的最短距离。它不使用作者的动态距离更新算法。所有非空和为零、长度≤13的系数组合均直接检查；不可达距离取 99。

首根由显式 24 个坐标置换重算；每个后继层从整个群中取所有严格递增可行值。独立生成的键与证据行一一对应，逐根与上游计数、生产汇总同时比较；检查前后输入哈希未变。每个核心重新扫描全部 625 个值产生候选，逐对核给定颜色，全部组合分母和直方图重算。

| 核验量 | 独立结果 |
|---|---:|
| 初始安全双块 / 首根 | 475 / 44 |
| 十二项前缀 | 8642 |
| 十四项完整节点 | 166195 |
| 含各层前缀的实际系数元组 | 369779256 |
| 十四项核心的全群候选循环 | 103871875 |
| 全部候选对 | 35295586 |
| 合法至多五着色 / 残余 | 166180 / 15 |
| 六元组合总分母 | 4961379937313 |
| 七元组合总分母 | 171921339634017 |
| 十五残余的六元组合分母 | 4905187140750 |
| 十五残余的七元组合分母 | 171681549926250 |

完整计算 6.5064033 秒，退出码 0。结果见 [verify_seven_fresh_core.json](C:/game/gameproject/showa100/math/2026-09-04_zhao_attack/evidence/verify_seven_fresh_core.json)。这些组合分母依旧不是对全部组合逐一搜索的宣称；五着色统一排除了非残余核心的全部六单项扩张。

十五残余另由 [verify_seven_fresh_bindings_h12.py](C:/game/gameproject/showa100/math/2026-09-04_zhao_attack/scripts/verify_seven_fresh_bindings_h12.py) 独立检查，未导入任何生产或旧审计模块。每个 V 展开为十二个实际位置，枚举全部 2^12 个位置掩码；总计 61440 个掩码，覆盖十五核的全部 10935 个重数系数模式。全部 1875 行目标表示逐行核对目标、系数范围、长度及真正最短性。矩阵行列式通过 F5 高斯消元重算，且逐支撑检查矩阵作用。

共同距离直方图为 `{0:1,1:6,2:19,3:36,4:35,5:19,6:8,12:1}`。输出另外保存每个目标的独立最短位置掩码，见 [verify_seven_fresh_bindings_h12.json](C:/game/gameproject/showa100/math/2026-09-04_zhao_attack/evidence/verify_seven_fresh_bindings_h12.json)。该检查连同哈希绑定共 1.6645083 秒，退出码 0。

## 4. 哈希与旧引理版本绑定

候选冻结清单的 6 个先前输入、9 个冻结新文件共 15 项，全部与当前字节一致。进一步重核 H12 证书、作者回放记录、生产元数据和独立计算输出中的输入绑定；本次记录 39 条显式哈希引用边和 26 个文件的当前哈希。作者回放记录只作为冻结工件核对，未执行它来替代独立验证。

旧 `proofs/hyperplane_dense12.md` 当前 SHA-256 是 `3bd7e246c77a212ebe353b26b99c866fe8c66c2595b89ba68047e1d9dd5d1f22`；原新上下文审计的输入 SHA-256 是 `64e56bea49d48e4e4f0054c188dc700da5b48865c358d346deec9274ae50b7fb`。本次核实 `evidence/dense12_audited_snapshot.md` 精确匹配后一个哈希，并逐行比较原快照与当前证明：只有第 3 行把“候选待验”改为“审计 CORRECT”，数学正文逐字相同。旧审计器及当时九核分类输入哈希也相符。因此旧手工引理接口有明确版本绑定。

有一项**不属于本候选依赖的旧记录差异**：同一旧审计还附带审核了 `proofs/lower_bound_18_B.md`，其记录哈希为 `0628c6686c6c87594f43d47b90a71f68e95272b737d9473ed1f6b72308d42729`，当前哈希为 `4bc5a79ae81e653e14866ce55320903c0d18c7ce48c151e0b96557c7985664f1`。本次确实发现并保存了该不匹配，没有声称旧审计的所有附带文件仍字节一致。十八项下界未被当前七双重值命题或稠密十二项手工引理使用，且不列于当前候选冻结清单；故它不构成此候选的证明缺口，本审计也不重新认证那份已变化的附加构造文本。

## 5. 复现与信任边界

复现本次新核验：

    pwsh -NoLogo -NoProfile -File scripts/verify_seven_fresh_core.ps1
    python -B scripts/verify_seven_fresh_bindings_h12.py

实际使用本机已配置的 PowerShell 7 和 Python 运行时；只写新审计输出。运行及上述逻辑审查足以认证收到版本的完整有限证明。本次没有运行 Lean 小内核检查，仍需信任纸面约化与独立整数程序的对应及运行环境。任何对冻结证明数学内容、核心证书或 H12 证书的后续实质修改都需要重新绑定和审核。

没有发现要求修改局部命题前提或补上承重引理的关键缺口。此 CORRECT 仅指上述局部排除及 GL3 分类，不是完整端点 A21/B20 的认证。
