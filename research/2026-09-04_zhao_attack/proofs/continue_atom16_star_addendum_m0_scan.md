# d=4、M=0 配对星强制核心：有界一层侦察

**STATUS: COMPLETE_BOUNDED_SCAN；不排除该分支，不改变完整 B 的 INCOMPLETE 状态。**

输入使用已冻结的星结构稿及 M1 附录。此处只处理 d=4、M=0，即 R 有四个互异值 `g_i=x+y_i`，T 不含任何 g_i。S 强制包含十三位置核心

    V = x · Π_{i=1}^4 g_i y_i^2.

若 `span{x,y1,y2,y3,y4}` 秩至多三，则 V 自身的十三位置已由 `D(F5^3)=13` 排除。秩四时，由于四个 y_i 角色完全对称，任取一个四元基后只有两类图表：四个 y_i 为基而 x 自由，或 x 与三个 y_i 为基而第四个 y_i 自由。每个自由向量完整遍历 F5^4 的 625 个值。两个图表可重叠，但覆盖无遗漏。

`scripts/continue_atom16_star_addendum_m0_scan.py` 不导入项目搜索代码。它先检查九个强制支撑值互异且非零，再按十三个实际位置计算精确最短子序列距离；对每个短零和自由核心，逐一测试全部 625 个可能下一值。加入 z 后仍无长度至多十四零和的充要条件是 `d_V(-z)≥14`。证据同时保存完整原始安全表和排除九个已有支撑后的 W 候选表。

| 基图表 | 自由向量 | 合格互异支撑 | 短零和自由核心 | 核心外安全下一值数量：核心数 |
|---|---:|---:|---:|---|
| 遗漏 x | 625 | 604 | 36 | 35:24，40:12 |
| 遗漏一个 y_i | 625 | 613 | 69 | 35:24，36:6，40:12，137:18，145:9 |

完整分母是 1250 个图表参数、1217 个合格互异支撑、105 个安全核心，以及每个安全核心全部 625 个下一值；没有时间截断。原始安全值数量与核心外数量的差来自已有支撑值是否可按位置再加一份，二者没有混用。

该表并不“小到可直接逐七后缀”。特别是第二图表有 27 个参数根仍有 137 或 145 个核心外安全值。任何后续完整计算都应先分类这 27 个稠密根的子空间/自同构结构，并同时强制 W 长七、总重复亏格至多三及 T 的总和方程；不能把本次单步候选扫描称为排除。

证据为 `evidence/continue_atom16_star_addendum_m0_scan.json`，保存每个安全参数根、强制实际序列、625 点距离直方图及全部安全值。没有启动后缀 DFS、随机搜索或 SAT。
