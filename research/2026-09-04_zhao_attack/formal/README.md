# Lean 检查范围

状态：LEAN_PARTIALLY_CHECKED。检查秩三核心的两个有限覆盖引理，以及十八项显式下界序列不存在≤13非空零和；没有形式化完整 A/B，也没有形式化四独立核心搜索。

工具链：Lean 4.30.0，commit d024af099ca4bf2c86f649261ebf59565dc8c622，已安装的独立 Std 工具链。没有更改工作区项目工具链、全局版本或下载 Mathlib。

实际命令：`C:/Users/UIO/.elan/bin/lean.exe +leanprover/lean4:v4.30.0 Rank3Core.lean`，退出码 0。

两个定理的公理依赖均为 `[propext, Quot.sound]`。文件没有 sorry、admit、新增 axiom 或 unsafe。全部证明使用 `decide`，不使用 native_decide。

编码对象：四个系数各在 Fin 4，即可选重数 0,1,2,3；三个坐标各在 Fin 5；核心为 e1³ e2³ e3³ (1,2,3)³。

- core_cover9：除 (1,4,2) 外，每个目标存在总系数≤9的表示。
- core_cover8：除 (1,4,2)、(4,0,0)、(0,4,0)、(0,0,4)、(4,3,2) 外，每个目标存在总系数≤8的表示。

注意：Lean 陈述是覆盖所需的单向保证，不宣称五个例外都不可用≤8表示，也不宣称距离分布表；完整自然语言证明只需要这两个单向保证。所有目标有≤12表示还需要给第一个例外显式系数 (3,3,3,3)，其坐标正好为 (1,4,2)。独立审计另核了更强的全部最短表示表。

LowerBound18.lean 的 noShortZero18 实际编译退出码0，输出 `does not depend on any axioms`。它遍历四个Fin4、两个Fin2、两个Fin3系数；四个模5坐标为0时，总长度要么0要么至少14。全部系数界恰好匹配实际序列重数。位置重排不影响是否存在零和，故这足以排除其全部≤13非空位置子集。精确位置计数另由独立Gray枚举检查。

早期版本把 reprCore 声明成普通 def，自动可判定实例未展开，编译失败；改成 abbrev 后实际通过。失败记录不被当作认证。
