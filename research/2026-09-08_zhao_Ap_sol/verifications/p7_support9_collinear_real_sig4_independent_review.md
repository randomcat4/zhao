# `p=7`、support-nine 共线实搜索签名四独立审计

STATUS: **CORRECT**

FINITE UNSAT SCOPE: 在

\[
G=C_7^3,
\qquad |B|=19,
\qquad |\operatorname{supp}B|=9
\]

的商层，假定支撑射影简单、单标签重数至多四，并冻结无序重数签名

\[
\boxed{(4,4,3,3,1,1,1,1,1)},
\]

则不存在既为零和原子、又能接入满足六位置商零和及精确
\(L_1/L_2/L_3\) 中层缺口条件的六尾。等价地，该签名对应的有限商层
存在性问题为 **UNSAT**。

本裁决不覆盖其他 support-nine 签名、support 大于九、68 个高度变量、
Hasse 条件、实际 \(C_7^4\) 序列、端点 \((A_7)\)、其他素数或一般
\(A_p\)。

审计日期：2026-09-09。审计前后的工作树 HEAD 均为
`77c51458cdb943495d084ee3bfc82ebf810b3dcf`。本次没有访问任务禁止的
目录，没有修改作者程序、冻结报告或总账，没有提交；唯一新增文件即
本审计报告。

## 1. 冻结对象、上游依赖与审计方法

直接被审对象稳定后的字节 SHA-256 为：

```text
p7_support9_collinear_real_search.py
67f6d6b84ffc9117a5a6e8cb6c2b5bb22a2497ad102e474d2e831c07d1fc9d78

p7_support9_collinear_real_verify.py
d5f4638286f392b104891199c7d38e8f9ac68bd21f47904184c883aabeb56d80

proofs/p7_support9_collinear_real_frontier.md
6ada4e29954d72279125ddbb064a8d0ae68266df92b05b5c93a0f2a7dfde24a1

p7_support9_collinear_real_sig4_report.json
cbdd160451f8ac9cf98573792c69bdb804c14bd1d73c91968b37feb3dacca87a
```

同时完整通读并核对了下列已认证上游证明与独立审计：

- `proofs/p7_m34_support9_frontier.md` 及
  `verifications/p7_m34_support9_frontier_independent_review.md`；
- `proofs/p7_support9_structure_pruning.md` 及
  `verifications/p7_support9_structure_pruning_independent_review.md`；
- 本共线搜索证明稿及前三个签名的独立审计。

本结论使用两个已认证的上游事实：任意九点射影支撑含三个共线点，
以及目标原子在任意二维子空间内的真实位置权重至多 11。本报告不把
这两个上游分类重复冒充为新证明。

作者核验器默认模式实际通过，输出四个分母
`(56, 118, 197, 282)`、显式加权换基轨道、固定部分与位置层差分、
冻结哈希和逐深度守恒均通过。该核验器导入作者模块，故这里只把它
作为 proof/verify/report 当前彼此一致的补充证据。

主独立复核是一个没有落盘、只用 Python 标准库的只读程序。它不导入
作者搜索器或核验器，从重数多重集、有限域算术和冻结 JSON 重新生成
profile 分母、显式换基轨道、报告流哈希、所有计数恒等，并对原子
前缀、平面打包、真实位置层、六重和集、强制末点和 singleton escape
做小规模差分。没有重跑全部约 \(2.6\times10^8\) 个 DFS 进入节点；
重型全量执行仍由确定性搜索器、逐 profile 冻结报告及其哈希承载。

此外只重放了承载完整候选的第 11、22 两个 profile，保持冻结全签名
运行使用的跨 profile 对称语义；随后捕获这两个候选的实际载荷，用不
依赖作者位掩码的集合型递推重新计算其全部固定基数和值层。

## 2. 282 个共线 profile 的完整覆盖

独立枚举从签名多重集中选择三个重数，降序赋给共线三点
\((Q,E_2,C)\)；从余下六个重数中枚举每个可能的线外锚 \(E_3\)
重数；再把最后五个重数的全部不同排列依次附着于严格递增的向量
编号。集合去重后恰得 **282** 行，无重复，并与 JSON 的
`profile_index=1..282` 逐项构成同一个集合。每一行排序后的九重数
都恰为冻结签名。

这个分母覆盖正确的几何量词。对任一目标支撑，先选一条含至少三个
支撑点的射影直线，把所选三点按重数非增命名；再从线外支撑中选重数
最大的点作 \(E_3\)。正规化后第三个共线点为

\[
C=(u,v,0),\qquad u,v\in\mathbb F_7^\times,
\]

所以每个 profile 有 36 个原始 \(C\)-frame。所选直线上还可能有
其他支撑点，故 \(m(E_3)\) 不必是余下六点的全局最大值；程序只对
第三坐标非零的余点施加 `multiplicity <= m(E3)`，与“线外最大”的
准确条件一致。

五个余点虽按重数优先遍历四个自由变量，`respects_known_order` 对
已经赋值的原 profile 下标执行双向严格编号比较。被强制的变量是最后
一个最小重数变量；本签名 282 行中其重数全部为 1。因此遍历顺序及
强制策略都没有弱化“最后五点按编号严格递增”的规范条件。

## 3. \(S_3/S_2\) 轨道与 5,382/10,152 加权覆盖

独立复核没有使用搜索器的三个闭式换位公式。对二维有序点列

\[
(1,0),\quad(0,1),\quad(u,v)
\]

直接穷尽保持三点重数的置换，并对新的前两点显式求换基坐标。逐
profile 得到的轨道数与 JSON 的 `collinear_c_orbits` 完全相同；每个
轨道的大小加总为 36。按固定三点重数分组如下：

| \((m_Q,m_{E_2},m_C)\) | profile 数 | 搜索代表 | 原始 frame |
|---|---:|---:|---:|
| \((4,4,3)\) | 6 | 126 | 216 |
| \((4,4,1)\) | 15 | 315 | 540 |
| \((4,3,3)\) | 6 | 126 | 216 |
| \((4,3,1)\) | 30 | 1,080 | 1,080 |
| \((4,1,1)\) | 60 | 1,260 | 2,160 |
| \((3,3,1)\) | 15 | 315 | 540 |
| \((3,1,1)\) | 60 | 1,260 | 2,160 |
| \((1,1,1)\) | 90 | 900 | 3,240 |
| 合计 | **282** | **5,382** | **10,152** |

故冻结运行的 5,382 个代表按轨道大小严格覆盖

\[
10,152=282\cdot36
\]

个原始 `profile × C-frame` 入口。三点全等时使用完整 \(S_3\)，恰
两点等重时只使用合法的 \(S_2\)，三重数互异时保留全部 36 个 frame。

换位会改变其余五个标签的向量编号次序，因此这个约化可能跨 profile。
完整 282 行对最后五个重数的全部不同排列封闭，变换后重新按编号排序
必回到同一完整入口表，故全签名运行没有漏轨。代码中唯一的对称开关是

```text
full_signature = (start_profile == 1 and stop == len(profiles))
```

并把这个布尔值直接传给每个 profile。任意真子区间都恢复全部 36 个
frame，不会在 partial shard 中偷用跨 profile 对称。反过来，若用
partial CLI 重跑第 11、22 行，会出现全 36 frame 下的对称重复，其
候选数本来就不应与冻结全签名的两个代表直接比较。

## 4. 原子、平面、真实位置与尾前缀门

### 4.1 固定部分与原子前缀

固定部分是 \((Q,E_2,E_3,C)\)，对应重数顺序
\((m_Q,m_{E_2},m_{E_3},m_C)\)。`fixed_forbidden_mask` 穷尽四个
固定纤维的全部非空有界系数和；固定部分已有零关系时立即拒绝。
自由前缀的 `reachable` 精确保存此前余纤维的全部有界系数和值。
加入 \(x^m\) 时枚举 \(jx\)、\(1\le j\le m\)，同时检查余点内部
零关系和与非空固定部分合成的零关系。强制末点尚未加入，所以命中
的关系在所有完成中都是真子多重集，剪枝安全。

独立集合实现以固定随机种子做了 400 次逐纤维扩张，将上述状态门与
完整系数笛卡尔积比较；拒绝判定及每个接受状态的可达和集全部一致。
代码检查还确认 `profile` 的顺序

```text
(m_q, m_e2, m_c, m_e3, m_0, ..., m_4)
```

在传入完整对象时被正确配成支撑顺序

```text
(Q, E2, E3, C, x_0, ..., x_4)
(m_q, m_e2, m_e3, m_c, m_0, ..., m_4).
```

不存在 \(C/E_3\) 重数错位。完整 singleton 域若非空，程序才调用
`bounded_atom`；该函数由后三维外的系数唯一强制前三个标准基系数，
排除全零和完整总和关系后，是这里的有界原子充要判据。

### 4.2 二维平面负载

程序维护 \(PG(2,7)\) 的 57 个二维子空间，每个非零值恰属于八个。
每个平面用四位保存实际位置负载，并在写入前拒绝超过 11 的更新；
所有已接受负载至多 11，所以没有跨四位域进位。

独立普通整数数组与同公式的打包更新比较了 1,338 次接受增广，逐次
负载及首次拒绝完全一致。负载上界 11 来自上游已认证的完整
\(C_7^2\) 分类，是目标原子的必要条件，不是把粗 Davenport 上界或
经验阈值当作证明。

### 4.3 真实位置固定基数层与六尾前缀

`add_layers` 对重数 \(m\) 使用系数 \(0,\ldots,m\) 更新
\(\Sigma_0,\ldots,\Sigma_{15}\)。在“和值是否可达”的语义下，这
严格等价于把同标签的 \(m\) 个副本作为不同真实位置逐一加入。独立
差分对 120 组随机纤维序列逐层比较两种实现，全部一致。

部分前缀 \(P\) 使用的单点尾域为

\[
E_1(P)=\{t\ne0:-t\notin\bigcup_{k=8}^{15}\Sigma_k(P)\}.
\]

完成只能增加每层可达和，故 \(E_1(B)\subseteq E_1(P)\)。若较大的
部分域上都没有允许重复的六项零和，后续完成不可能恢复。程序用

\[
0\in6E\iff3E\cap(-3E)\ne\varnothing
\]

精确判定该放宽问题；100 个随机小域上与允许重复的无序六多重集直接
枚举一致。另在 36 个随机长度十九零和位置序列上，从高侧
\(-\Sigma_{8..15}\) 与补集低侧 \(\Sigma_{4..11}\) 分别计算 singleton
域，结果逐例相同，并逐例确认完整域包含于随机真前缀域。

## 5. 强制末点与报告计数守恒

四个自由余点之后，末点重数 \(m_f=1\)；一般公式为

\[
x_f=-m_f^{-1}\sum_{x\ne x_f}m_xx.
\]

因为 \(1\le m_f\le4<7\)，逆元总存在。250 个随机小例中直接穷尽
343 个群元，公式每次都给出唯一解。代码随后依次检查强制值非零且
不在固定射影线上、不与自由余点同射影线、线外最大条件、原编号次序
及平面负载，全部通过后才计为完整零和候选。

JSON 的 282 行在每个深度都满足

```text
entered = atomic_pruned + plane_pruned + tail_pruned + survived.
```

逐行重加与顶层聚合完全相同：

| 深度 | 进入 | 原子剪枝 | 平面剪枝 | 六尾剪枝 | 幸存 |
|---:|---:|---:|---:|---:|---:|
| 1 | 426,276 | 252,312 | 24,060 | 0 | 149,904 |
| 2 | 20,795,726 | 18,707,033 | 556,003 | 918 | 1,531,772 |
| 3 | 166,382,646 | 161,598,486 | 3,937,941 | 26,919 | 819,300 |
| 4 | 72,286,987 | 67,849,165 | 4,366,483 | 766 | **70,573** |

5,382 个 \(C\)-代表中，1,320 个在固定部分原子门被拒绝。所有 282
行的深度四末点分流也分别守恒，聚合为

\[
\boxed{70573=45550+25008+0+13+0+2},
\]

右边依次为固定线或零值拒绝、与自由余点射影重复、线外最大拒绝、
编号次序拒绝、平面容量拒绝和完整零和候选。它是完备分割，不是拒绝
原因的下界。

报告文件哈希及逐 profile 规范 JSON 流哈希分别复现为

```text
cbdd160451f8ac9cf98573792c69bdb804c14bd1d73c91968b37feb3dacca87a
9a4b9eb28e714e5a82dfd574c7a832291b81cef4ecdc525e360fe87d109e589f
```

空 `position_reports` 的规范 JSON 列表哈希为

```text
4f53cda18c2baa0c0354bb5f9a3ecbe5ed12ab4d8e11ba873c2f11161202b945
```

## 6. 两个完整候选的独立 singleton-empty 核验

只有第 11、22 两行产生完整零和候选，各恰一个。按冻结全签名对称
模式单独重放这两行，完整候选流哈希逐一复现。通过只读回调捕获候选
载荷后，另用集合型有界系数递推从九个实际纤维重新计算全部真实位置
固定基数层，结果如下：

| profile | profile 参数 | 支撑 ID | 实际支撑顺序重数 | 候选 SHA-256 | \(|E_1(B)|\) |
|---:|---|---|---|---|---:|
| 11 | `(4,4,1,3,3,1,1,1,1)` | `(49,7,1,140,50,155,204,301,307)` | `(4,4,3,1,3,1,1,1,1)` | `df268aa29449f61fccc13c5a9b44afa6282441e0ce0ee5b1bbd807d9ea883dff` | 0 |
| 22 | `(4,3,3,4,1,1,1,1,1)` | `(49,7,1,56,104,155,204,295,337)` | `(4,3,4,3,1,1,1,1,1)` | `9c090b33cc05e793b36397978562353830cf410b1a8e94f4bec6fb7070d7bebe` | 0 |

表中 profile 参数第三、四项是 \((m_C,m_{E_3})\)，而“实际支撑顺序
重数”跟随 \((Q,E_2,E_3,C,\ldots)\)，所以两列的换位是预期接口，
不是报告错配。

两候选都满足加权总和为零并有九条不同射影支撑线。更关键的是，独立
递推在两例中都得到

\[
\left|\left(\bigcup_{k=4}^{11}\Sigma_k(B)\right)
\setminus\{0\}\right|=342,
\]

即 \(C_7^3\) 的全部非零元素都已被 \(\Sigma_{4..11}(B)\) 覆盖。因
\(\sigma(B)=0\)，这与高侧定义

\[
E_1(B)=\{t\ne0:-t\notin\Sigma_{8..15}(B)\}
\]

完全相同，故两候选的 singleton escape 域确实都为空。任何合法六尾
的每一个标签都必须属于该域，所以两候选在一元必要条件上已经严格
排除。

这两个对象只是“强制末点有效、满足规范和前缀必要门的完整零和候选”，
报告没有把它们称为原子。先检查空 singleton 域、再决定是否调用完整
原子测试是安全的：若其中任何对象是目标原子，它仍会因空域不可能接
尾；若不是原子，本来也不属于目标。因此无须为了有限 UNSAT 先分类
这两个对象的原子性。

报告分拆逐行及聚合均严格为

```text
complete_zero_sum_candidates         2
complete_full_singleton_empty_pruned 2
complete_full_singleton_nonempty     0
complete_atoms                       0
exact_tail_survivors                 0
```

第 11、22 行的 `atom_sha256` 和 `exact_tail_sha256` 都是标准空字节流
哈希

```text
e3b0c44298fc1c149afbf4c8996fb92427ae41e4649b934ca495991b7852b855.
```

因此“两个 complete 都 singleton-empty，且无 atom/tail survivor”
不仅是顶层计数声明，还由两条候选载荷的重放、payload 哈希及独立真实
位置和值层计算逐一核实。

## 7. 四个新签名及 support-nine 总口径

不导入作者 profile 生成器，从九个正重数、每项至多四、总和十九的
无序分拆重新生成全部 support-nine 签名，恰得 13 个；逐签名共线
profile 分母总和恰为 5,741。

本共线搜索的四个新签名分母为

\[
56+118+197+282=653.
\]

旧证书关闭的两个轻签名
\((3,2,2,2,2,2,2,2,2)\) 与
\((4,2,2,2,2,2,2,2,1)\) 在同一共线接口下分别占 7 与 43 个
profile，共 50 个。因此当前精确汇总为

\[
\boxed{6/13\text{ signatures closed}},
\qquad
\boxed{703/5741\text{ collinear profiles closed}},
\]

并且

\[
5741-703=5038,
\]

即仍有 **7 个签名、5,038 个共线 profile 开放**。这些 profile 是
规范覆盖入口，不是唯一 \(GL(3,7)\) 轨道；并列选择造成的重复不影响
上述闭合/开放分母，但不能把 profile 数误称为轨道数。

## 8. 最终裁决与停止线

未发现 profile 漏枚举、\(S_3/S_2\) 轨道漏覆盖、partial shard 误用
对称、向量次序弱化、\(C/E_3\) 重数错配、原子前缀误剪、平面打包
错误、真实位置压缩、六尾重复标签丢失、强制末点分流不全、报告哈希
漂移，或把两个 complete 零和候选误报成原子的问题。

因此最终裁决为 **CORRECT**：冻结签名
\((4,4,3,3,1,1,1,1,1)\) 的 282-profile、5,382-代表、10,152-原始
frame 有限商层搜索严格 UNSAT。该结果只关闭这一个完整无序签名；
全局 support-nine 当前是 6/13 签名闭合，真实高度系统、\(A_7\) 与
一般 \(A_p\) 仍为 **INCOMPLETE**。
