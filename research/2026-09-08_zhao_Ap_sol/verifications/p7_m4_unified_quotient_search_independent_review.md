# `p7_m4_unified_quotient_search` 独立审稿

STATUS: CORRECT

JUSTIFICATION:

1. **冻结接口足以支撑非零标签量词。** 最新 `assumptions.md` 已把 (F3-NET) 明确冻结为从 `route_a_atom_line_rigidity.md` 导出的公共接口：对长度 $3p+4$ 的实际零和原子 $Z$，全部第三族块的公共核心为空，且每个第三族块的商补集都是 $C_p^3$ 中的原子。候选取固定的第三族块 $T$。对任意 $t\in T$，公共核心为空给出另一个不含 $t$ 的第三族块 $A_t$；其商补原子 $Z\setminus A_t$ 含位置 $t$。若 $\bar t=0$，单点 $\{t\}$ 就是这个长度至少 17 的商原子的非空真商零子集，矛盾。因此每个 $T$ 位置的商标签均非零。该推理正好使用 (F3-NET) 的两部分，没有把“任意裸六项多重集”偷换成完整第三族网络。本审稿按任务范围把 (F3-NET) 当作冻结的已认证派生接口使用；其来源与适用对象已在 `assumptions.md` 第 69--87 行精确标明。

2. **九至十六元中间空档及其量词使用正确。** 候选引理 1 现已显式假设 $Z\subseteq R$、$|Z|=3p+4=25$、$\sigma(Z)=0$。若 $t\in T$ 为非零标签，且某个 $W\subseteq B$ 满足 $8\le |W|\le11$ 与 $\bar\sigma(W)=-\bar t$，则 $K=W\mathbin{\dot\cup}\{t\}$ 是长度 9--12 的商零位置集，$Z\setminus K$ 是长度 13--16 的商零位置集。二者都是 $R$ 的非空位置子集且长度不超过 $2p+2=16$，故 (SQ) 分别给出实际和 $ra$、$sa$，其中 $r,s\in\{1,2,3\}$。由 $\sigma(Z)=0$ 得 $(r+s)a=0$。冻结反例边界推出 $a\ne0$：否则 $S=a^3R$ 含一项实际零和；所以 $a$ 的阶为 7，而 $2\le r+s\le6$，矛盾。`middle_quotient_gap.md` 中从群等式到系数同余的承重步骤已显式包含并正确证明 $a\ne0$。

3. **固定式 (24) 的商原子性已独立重放。** 脚本实际载入的有序 19 位置多重集为
   $$
   (100)^4(106)^3(001)(010)(016)^3(150)(160)^4(456)(546)
   \subset \mathbb F_7^3.
   $$
   我直接枚举了全部 $2^{19}$ 个位置子集：全体和为零，524286 个非空真子集均非零。因此该 $B$ 确为商原子。脚本对十五位置 $Q$ 所做的 98303 个 $b=0,1,2$ 目标测试也与直接判据等价：任何 $b=3,4$ 的非空真零和子集取其在总零和 $B$ 中的补集，都会变成 $b=1,0$ 的已排除情形。

4. **八至十一层覆盖与见证规范完全一致。** 独立于候选的动态规划，我逐个位置掩码复算得到
   $$
   (|\Sigma_8(B)|,|\Sigma_9(B)|,|\Sigma_{10}(B)|,|\Sigma_{11}(B)|)
   =(305,312,312,305).
   $$
   四层并集恰为 $\mathbb F_7^3\setminus\{0\}$ 的全部 342 个值，且零不在并集中。按“先子集大小、再十九位整数掩码”取每个和值的首见证，大小直方图为 $(305,23,11,3)$；再按和值的字典序进行无空白 JSON 序列化，SHA-256 为
   `7fefc772ca2ad2f70ac2f64491ccd317c38b93f88bc30f07a35349b6dbd7dfdd`，与候选一致。该哈希规范的是固定的有序 19 位置表；轨道外推所保持的是商原子性和完全非零覆盖，而不是字节级哈希本身。

5. **从任意允许的 $T$ 标签到矛盾的量词闭合。** (F3-NET) 先保证每个 $t\in T$ 非零；完全覆盖再为 $-\bar t$ 给出一个大小 8--11 的真实位置见证 $W\subseteq B$；引理 1 随即产生长度 9--12 的被禁商零位置集。因此甚至第一个 $T$ 位置就已矛盾，不需要枚举其余五个标签或求高度。结论覆盖固定 $B$、任意满足完整第三族网络的六位置 $T$ 以及任意高度抬升。任意可逆商线性变换保持零、非零、子集大小、商原子性及全部非零和值覆盖，位置置换也保持这些性质，故对固定 $q$ 的商自同构轨道与位置置换轨道外推合法。

6. **参数化商骨架诊断没有发现接口漏检或误报。** `diagnose_unified_interface(t_labels)` 现在检查恰有六个标签及其总商和为零，并在同一 25 位置顺序上返回完整诊断和 `rejected` 布尔值。12+13 折半枚举在 200 个确定性小规模实例上与全子集暴力枚举逐掩码一致。九至十二元商零集的存在直接触发拒绝；总商和为零使十三至十六元禁层与其九至十二元补集完全等价。对六至八元短块，补集长度为 17--19；若补集非原子，则一个零和真子集及其在补集内的补集至少有一边大小不超过 9，所以搜索一至九元商零子集是完备的。全部长度二至八的自动商零块都在约束表中，因此多块不交族第一次使总支撑超过 8 时，此前并集本身也是表内短块，成对检查足以捕获该冲突。`rejected = bool(medium or disjoint_gap_pairs or empty_domains or heavy)` 已显式包含中间禁层、不交空档冲突、空高度域和重纤维障碍四类直接拒绝条件，与正文的判定语义完全一致；其中不交冲突其实还会被 `medium` 蕴含，但当前返回语义不依赖这项冗余。仅删除高度值 3 而仍有其他允许高度时，正确地把收缩后的域留给下一层高度求解。重纤维检查遍历完整 25 位置中每个重数至少四的标签及全部六个指定单值窗。旧式 (25) 回归返回 29658 个中间禁集、168573 对不交冲突、1893 个空高度域、7 个重纤维障碍及 `rejected=True`，不会被误记为通过。全零裸 $T$ 返回 `rejected=False` 也不构成误报：该函数只表示当前商层障碍为空，正文明确不声称这种裸标签满足 (F3-NET)，后续仍须进入高度层。

7. **证书边界表述准确。** 该证书只排除显式固定 $B$ 及其上述性质轨道中的完整 $F_3$ 网络扩张；它没有枚举全部六位置 $T$，也不需要这样做。它没有分类其他长度 19 商原子，没有排除 $D_B\ne\varnothing$ 的其他骨架，且不能外推到其他素数。候选已明确写出：全 $p=7,m=4,0001$ 分支仍为 **INCOMPLETE**。有限证书的复现状态只对应文档给出的普通 Python 运行，即断言检查启用；优化模式 `python -O` 不属于本次认证命令。
