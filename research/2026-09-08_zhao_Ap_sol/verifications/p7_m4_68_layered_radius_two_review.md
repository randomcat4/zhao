# (p=7,m=4,0001,s=6) 两步分层证书同上下文自检

STATUS: **SELF_CHECK_PASSED / FRESH REVIEW PENDING / FINITE RADIUS-TWO ONLY**

## 复核结论

同一工作上下文中的第二实现复现了
`proofs/p7_m4_68_layered_radius_two.md` 的有限结论，且量词边界
写得正确：它穷尽的是固定未标记长度 19 原子骨架的等和二位置变异
半径二邻域，不是所有长度 19 原子，也不是完整 68 标量规范空间。
这不是全新上下文审稿票；最终 `CORRECT` 状态须由另一上下文给出。

同上下文自检得到：

- 精确距离 (0,1,2) 的商零原子数为 ((1,8,530))；
- 两次扩张候选并集大小为 ((6840,43923))；
- 最大商纤维不超过四、且至少有一个四重纤维的未标记原子数为
  ((1,5,4))；
- 把每个四重商值分别指定为 (q) 后，带指定纤维分支数为
  ((2,10,8))，共 20 支；
- 对每一支，完整单点逃逸域

  [
  E_1(B)=\{t\ne0:-t\notin\bigcup_{k=8}^{15}\Sigma_k(B)\}
  ]

  均为空。因此 20 支都在商标签层严格无解，没有任何高度抬升进入
  Hasse/实际原子层。

## 算法独立性

主求解器按实际位置对删除，并枚举首个插入标签来生成变异候选。
自检脚本不导入主求解器，改用完整多重集的删除值对和“给定和值的
无序插入值对”表生成同一邻域。它另行实现有限域整数加法表、1 至
9 元原子动态规划及 8 至 15 元定长子集和动态规划。

这两种候选生成方式复现相同集合哈希。逐层原子哈希为

```text
53d459902b63737338d05bc7cf8db1accd7a515615c7031e0116dd0c4ca7df49
0b29f69959a8d85fc1a6b573b15b98dc13f44ca8577e491875398810dbaa82e2
d5f2f8479040c178176882ec86d8f8dbb498d22a987c60ef58a1ba146e08791d
```

两次候选并集哈希为

```text
a123ad016325cbef71acb4da4fd69f662fa8056c9e74ea4c02703c22c3866ad9
6e9f25f3f813001acb3605154e019fa8eef50d65058a9f01520b77b4ad613f92
```

机器报告的实际文件 SHA-256 为

```text
481cf1f7bdcd9b36b08221b1ad99b0f3da9c1be2eda64e3effd24ec0861e699e
```

## 与通用尾条件化谱的接口核对

`proofs/p7_tail_conditioned_middle_spectrum.md` 对 (s=6) 证明：在
(B) 是长度 19 商原子的前提下，完整中层空档等价于尾单点、尾对、
尾三元组三行

\[
\Sigma_j(T)\cap
\left(-\bigcup_{k=9-j}^{16-j}\Sigma_k(B)\right)=\varnothing,
\qquad j=1,2,3.
\]

主求解器实现同一指标范围，并额外检查 (j=4,5)；由
(\bar\sigma(B)=\bar\sigma(T)=0) 的补集对称，后两行分别与
(j=2,1) 等价，所以没有增加未经证明的限制。当前 20 支已在
(j=1) 行关闭，故尾对、尾三元组、短块高度窗及后续完整核验层
均不需要参与承重结论。

通用稿对 (s=7,8) 的推广也成立，但本证书只枚举长度 19 的 (B)，
即 (s=6)。不得把这里的计数或空逃逸域外推到长度 18、17 补原子。

## 完整约束入口与未覆盖范围

主脚本的 `--instance` 入口把完整赋值交给既有 25 位置核验器；后者
从统一标签自动重建全部二至八元短块，精确拒绝九至十二元中层，检查
全 Hasse/交网络、固定 (B) 商原子、实际 (Z) 原子及每个
(F_3) 补原子的内部商零子集。既有三个回归程序均再次通过。

但这只是保证“到达最终层的完整赋值会被严格核验”。主程序没有遍历
全部 68 个自由标量，也没有分类邻域外的商原子。因此最终状态必须
保持：**有限半径二排除正确；完整 (p=7,m=4,0001,s=6) 分支仍
INCOMPLETE。**
