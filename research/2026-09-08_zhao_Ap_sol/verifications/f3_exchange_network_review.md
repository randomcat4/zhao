# 多补原子交换网络独立验缝

STATUS: CORRECT_AFTER_CORRECTION

审核范围为 `proofs/f3_exchange_network.md`；不认证完整 ROUTE-A4 或
一般 \(A_p\)。

逐点真实度双计数、每个 \(F_3\) 块的近 Davenport 补原子、不同
\(F_3\) 块间二至十四项差关系及交换闭包均正确。特别地，\(p=7\)
时 \(|P|\le15\le16=2p+2\)，(SQ) 仍适用。

初稿把 \(\binom{M_p}{2}\) 个块对索引的关系实例写成同样多条彼此
不同的有符号关系；后者没有证明。现已修正为只计无序块对实例，
不主张关系序列不同。修正后结论为 CORRECT。
