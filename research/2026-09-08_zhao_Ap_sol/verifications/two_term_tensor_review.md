# 二项块与张量边界独立验缝

STATUS: CORRECT

审核范围为 `proofs/two_term_transversal.md`、
`proofs/design_tensor_boundary.md` 及主稿 (26e)、(28a)--(28c)；
不认证完整 ROUTE-A4 或一般 \(A_p\)。

核对通过：

1. 二项 \(a\) 块横截 \(\mathcal F_3\) 后，三种交类的带符号总数
   确为 \((1/20,1/20,-1/10)\)，继而
   \((d_1,d_2,d_3)=(1,-1/2,1/10)\)。普通块数下界均由整数剩余
   类的绝对值严格推出。
2. 值配对图的完全二部与半值完全图分解无遗漏；每分量边数至多
   \((p-4)\) 倍匹配数，包含 \(p=7\) 的取等边界。
3. 全局横截双计数中，每块至多覆盖 \(\binom73=35\) 个横截外三元组，
   (26e) 正确。
4. 三阶张量在重复指标处分别退化为已有二点、一点式；收缩符号与
   线性依赖正确，没有产生伪造的新约束。
5. 五列仿射秩证明对 \(k=6,7,8\) 及所有 \(p\ge7\) 成立；三次以下
   坐标矩看不见高度的显式例子正确。

审稿建议的非承重记号清理已完成：定义了 \(T_r^{(0)}\)，并把总和
为零的 \(q\) 限定为实际商坐标。
